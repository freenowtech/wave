import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { Logo } from '../Logo';

<Meta title="Essentials/Logo" component={Logo} />

export const Template = args => <Logo {...args} />;

# Logo

<Canvas>
    <Story name="Logo">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Logo" />

## Stories

### Basic

<Canvas>
    <Story
        name="Basic"
        args={{
            variant: 'default'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Inverted Monochrome

<Canvas style={{ backgroundColor: '#001E3E' }}>
    <Story
        name="Inverted Monochrome"
        args={{
            variant: 'default',
            inverted: true,
            monochrome: true
        }}
        parameters={{
            backgrounds: {
                default: 'dark'
            }
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Business Monochrome

<Canvas>
    <Story
        name="Business Monochrome"
        args={{
            variant: 'business',
            monochrome: true
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
