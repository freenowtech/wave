import { StyledSystemLinks } from '../../../docs/StyledSystemLinks';
import { Text } from '../Text';
import { Canvas, Story, ArgsTable, Meta } from '@storybook/blocks';

# Text

<Meta
    title="Components/Text"
    component={Text}
    argTypes={{
        as: {
            name: 'as',
            description: 'HTML tag to use',
            defaultValue: 'span',
            table: {
                type: { summary: 'string' },
                defaultValue: { summary: 'span' }
            },
            control: {
                type: 'text',
                value: 'span'
            }
        }
    }}
/>

export const Template = args => <Text {...args} />;

The Text component is a wrapper component that will apply typography styles to the text inside.

-   Use different headlines and copies to structure your content hierarchically
-   Avoid using sizes outside the given range
-   Text default size is medium

<Canvas>
    <Story
        name="Default"
        args={{
            children: 'This is text'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Usage

By default, `Text` uses the primary color, which is the color for most interface text, whether body or headlines.

Use the `secondary` color for supplemental information with diminished contrast (often, the “gray text”).
Like form microcopy, captions, helper text, and other medium emphasized text elements.

Use the `disabled` color for text in blocks, form elements, or other components that are not available
to a user at the moment (due to either permissions, dependencies, or pre-requisites) and
need to feature “incidental” states.

## Properties

<ArgsTable of={Text} exclude={['forwardedAs', 'theme', 'ref']} />

<StyledSystemLinks
    component="Text"
    supportedProps={['margin', 'fontSize', 'fontWeight', 'fontFamily', 'textAlign']}
    mt={2}
/>

## Stories

### Disabled

<Canvas>
    <Story
        name="Disabled"
        args={{
            children: 'I am disabled',
            as: 'p',
            disabled: true
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Secondary

<Canvas>
    <Story
        name="Secondary"
        args={{
            children: 'I have secondary color',
            as: 'p',
            secondary: true
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Inverted

<Canvas style={{ background: '#001E3E' }}>
    <Story
        name="Inverted"
        args={{
            children: 'I am inverted',
            as: 'p',
            inverted: true
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Using arbitrary HTML tags

<Canvas>
    <Story
        name="Use paragraph tag"
        args={{
            children: 'Use paragraph tag',
            as: 'p'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Using fontSize alias

<Canvas>
    <Story
        name="fontSize alias"
        args={{
            children: 'Using fontSize alias you can set (small, medium or large)',
            as: 'p',
            fontSize: 'small'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### With arbitrary styles

<Canvas>
    <Story
        name="styles"
        args={{
            children: 'You can apply responsive styles',
            as: 'p',
            fontSize: [1, 'medium', 'large'],
            fontWeight: 'semibold',
            marginTop: 3
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
