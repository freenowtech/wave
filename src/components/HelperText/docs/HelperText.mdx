---
name: HelperText
menu: Components
route: /components/helper-text
---

import { Playground } from 'docz';
import { ItemWrapper } from '../../../../docs/components/ItemWrapper';
import { Input } from '../../Input/Input';
import { HelperText } from '../HelperText';
import { HelperTextPropsTable } from './HelperTextPropsTable';

# HelperText

This component is a support for Input, Textarea and Dropdown components to display error feedback
for an users action or to provide further information in advance.

-   HelperText could be shown either for a single or a cluster of sources (e.g. displaying an error message for two inputs)
-   Occurring error message will replace existing hint notifications
-   They are placed underneath its regarding source and they are left align
-   In case of multiple error notifications you should additionally show an error banner to provide global feedback

## Properties

<HelperTextPropsTable />

`HelperText` also supports all the properties of the [`Text`](/components/text).


## Variants

### Single Input

<ItemWrapper>
    <div>
        <Input placeholder="Enter something" />
        <HelperText mt="1">127 Characters left</HelperText>
    </div>
</ItemWrapper>
<ItemWrapper inverted>
    <div>
        <Input inverted placeholder="Enter something" />
        <HelperText inverted mt="1">
            127 Characters left
        </HelperText>
    </div>
</ItemWrapper>

```jsx
<Input placeholder="Enter something" />
<HelperText mt="1">127 Characters left</HelperText>
```

### Multiple Inputs

<ItemWrapper>
    <div>
        <Input mr="1" placeholder="First name" />
        <Input placeholder="Last name" />
        <HelperText mt="1">Additional info that spans multiple input sources</HelperText>
    </div>
</ItemWrapper>
<ItemWrapper inverted>
    <div>
        <Input inverted mr="1" placeholder="First name" />
        <Input inverted placeholder="Last name" />
        <HelperText inverted mt="1">
            Additional info that spans multiple input sources
        </HelperText>
    </div>
</ItemWrapper>

```jsx
<Input mr="1" placeholder="First name" />
<Input placeholder="Last name" />
<HelperText mt="1">Additional info that spans multiple input sources</HelperText>
```

### Error

<ItemWrapper>
    <div>
        <Input placeholder="Enter something" />
        <HelperText mt="1" variant="danger">
            127 Characters left
        </HelperText>
    </div>
</ItemWrapper>
<ItemWrapper inverted>
    <div>
        <Input inverted placeholder="Enter something" />
        <HelperText inverted mt="1" variant="danger">
            127 Characters left
        </HelperText>
    </div>
</ItemWrapper>

```jsx
<Input placeholder="Enter something" />
<HelperText mt="1" variant="danger">127 Characters left</HelperText>
```
