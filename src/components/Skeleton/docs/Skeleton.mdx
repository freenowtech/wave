---
name: Skeleton
menu: Components
route: /components/skeleton
---

import { Playground } from 'docz';
import { Skeleton } from '../Skeleton';
import { SkeletonPropsTable } from './SkeletonPropsTable';
import { StyledSystemLinks } from '../../../docs/StyledSystemLinks';
import { Combination } from '../../../docs/Combination';

# Skeleton

Provide a placeholder while you wait for content to load, or to visualise content that doesn't exist yet.

## Properties

<SkeletonPropsTable /><br/>

<StyledSystemLinks component="Skeleton" supportedProps={['layout', 'margin', 'borderRadius']} />

## Examples

<Playground>
    <Skeleton width="200px" animated />
</Playground>

## Combinations

<Combination animated={[false, true]}>
    {(props, i) => <Skeleton key={i} {...props} />}
</Combination>
