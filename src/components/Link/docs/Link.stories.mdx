import { Canvas, Story, ArgsTable, Meta } from '@storybook/addon-docs';
import { StyledSystemLinks } from '../../../docs/StyledSystemLinks';
import { Link } from '../Link';

<Meta
    title="Components/Link"
    component={Link}
    argTypes={{
        inverted: {
            defaultValue: 'false',
            control: 'boolean'
        },
        fontSize: {
            options: [0, 1, 2, 3, 4],
            defaultValue: 2,
            control: 'select'
        }
    }}
/>

export const Template = args => <Link {...args} />;

# Link

The Link component styles anchor tags with default blue styling and hover text decoration. Link is used for destinations, or moving from one page to another.

-   Use links to navigate on and between pages and flows
-   For interactions consider Buttons

<Canvas>
    <Story
        name="Default"
        args={{
            href: 'https://free-now.com/',
            target: '_blank',
            children: 'https://free-now.com/'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

## Properties

<ArgsTable of={Link} />

<StyledSystemLinks component="Link" supportedProps={['margin', 'fontSize', 'textAlign']} />

## Stories

### Sizes

<Canvas>
    <Story
        name="Large"
        args={{
            href: '#',
            children: 'Link large (default)'
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Medium"
        args={{
            href: '#',
            children: 'Link medium',
            fontSize: 1
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Small"
        args={{
            href: '#',
            children: 'Link small',
            fontSize: 0
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

### Inverted

<Canvas style={{ background: 'rgb(0, 30, 62)' }}>
    <Story
        name="Inverted"
        args={{
            href: '#',
            children: 'Link large (default)',
            inverted: true
        }}
        parameters={{
            backgrounds: {
                default: 'dark'
            }
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
